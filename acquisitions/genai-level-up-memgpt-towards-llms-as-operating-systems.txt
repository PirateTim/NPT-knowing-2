Welcome to Genai Level Up, the podcast where we decode the future of artificial intelligence, one breakthrough at a time. >> Whether you're just starting your AI journey or you're a seasoned expert, you've come to the right place. [music] Our mission is to make the complex world of generative AI accessible to everyone, everywhere. >> [music] >> In each episode, we dive deep into the foundational concepts, the latest research, [music] and the transformative applications that are shaping our world. It's time to get smarter [music] faster. Let's level up together. [music] >> [music] >> Have you ever been, you know, deep in a chat with one of these really powerful AIs? Maybe you're mapping out some complex project or, I don't know, drafting a big document and then suddenly it just completely loses the thread. You you told it something critical just a few minutes back and boom, it's asking for it all over again. It's frustrating. >> Oh, absolutely. It's the uh the classic problem, isn't it? The Achilles heel of these large language models that short-term memory, we call it the limited context window, right? >> They can do these incredible calculations, generate amazing text, >> but they're stuck with processing only what fits in that fixed input block. >> Sometimes just a few thousand tokens, maybe up to a couple hundred thousand now, but still fundamentally limited. >> Yeah. It really feels like you're talking to this genius who like resets every 30 seconds. Yeah. their mental slate just gets wiped clean. Exactly. >> And that really holds them back, especially for anything longterm or when you're trying to get them to understand, say, a massive company database or something. >> And it's a tough constraint. I mean, if you look at a lot of the open source models, you might only get a few dozen back and forths before you hit that wall. >> So, the obvious first thought is always, okay, just make the window bigger, >> right? >> We've seen that, right? 128K tokens, 200k, trying to push it further. But uh that's where you went into the computational reality check making that window bigger directly. Well, it leads to a quadratic increase in the time and memory needed. >> Quadratic. Ouch. >> Yeah. It comes down to the self attention mechanism, the core of the transformer architecture. It just gets incredibly expensive fast. Both in terms of compute power and frankly cost, >> right? And what's really interesting is that recent research shows even when you can afford a huge context window, the models often don't even use it all effectively, they kind of get lost in the middle. >> Diminishing returns, basically. >> Precisely. Diminishing returns. They struggle to pinpoint the crucial info when it's buried in a massive block of text. >> Okay. So, if just making the window bigger isn't the magic bullet, it's too expensive and maybe not even that effective, we need a smarter approach, something different. We need a way to give the LLM the illusion of having infinite memory without actually changing that fundamental architecture. >> And that's where this paper comes in, the one we're diving into today, MEMGPT, towards LLMs as operating systems. >> Exactly. The researchers had this brilliant insight. They looked back at well decades old computer science. How did engineers solve the problem of applications needing more memory than the physical RAM available on a machine? Ah, the classic operating system solution, virtual memory. >> You got it. Virtual memory paging. It's such a neat analogy. >> So, break that down for us. How does that old OS idea apply here? >> Okay, think about your computer's operating system. It makes your machine seem like it has way more memory, maybe gigabytes more than the actual physical RAM chips installed. >> By using the hard drive. >> Exactly. It cleverly swaps data or pages it back and forth. It keeps the currently needed stuff in the fast RAM and moves the less immediately needed stuff out to the much slower but much larger disk storage. >> And the application running doesn't even know this is happening. Usually the OS manages it all behind the scenes. >> Precisely. Megpt basically takes that core idea of virtual context management and adapts it for LLMs. It uses the model's own ability to call functions >> like API calls. >> Yeah, essentially function calls. It uses those abilities to let the LLM manage its own context window. It pages information in and out between its limited main context, the prompt window, and much larger external storage. >> Whoa. Okay, so the LLM isn't just passively receiving a prompt anymore. It's actively managing its own workspace. >> That's the key shift. It becomes the manager. It decides, okay, this piece of info is important right now. Bring it in. This older stuff, page it out to storage, but remember where it is. That is a really fundamental change in how we think about interacting with these models. So if the LM's fixed input is like the RAM, how did MEGPT structure this virtual RAM? Operating systems divide up memory. Right? >> They did exactly that. They defined a memory hierarchy very much like an OS. It's split into two main parts. Main context which is like the physical RAM, the immediate tokens, the LLMC's and its prompt. Okay. an external context which is like the disk storage. Vast amounts of information stored outside that immediate window. >> Got it. Let's dig into the main context first. The virtual RAM. What's in there? >> It's divided into three contiguous sections. First, you have the system instructions. Think of this as readonly memory. It's static >> like the BIOS or the colonel's core rule >> sort of. Yeah. It contains the fundamental rules for the MEGPT agent, its personality, its overall goal, and crucially the instructions on how to use the special MEGPT functions for managing memory, saving things, retrieving things. This part doesn't change during a conversation. >> Okay, fixed rules. What's next? >> Next is the working context. This is super interesting. It's a fixed size readwrite area. Think of it like the LLM's personal scratch bot or notebook. >> Read. So the LLM can change what's in there. >> Exactly. This is where it stores key facts, evolving information about the user or the state of the current task like uh current goal is to draft section two of the report or maybe personal details like user prefers concise answers. >> And you said the LM can self-edit this part. That seems powerful. >> It is. And this is a huge piece of megpt. If during the conversation the user says something that updates a key fact, maybe their job title changes or like in their example they mention a breakup. Ah, the boyfriend named James becoming ex-boyfriend named James example from the paper. >> That's the one. >> Yeah. >> The LLM guided by its system instructions recognizes this is an important update. It then autonomously calls a MEGPT function on itself to modify that entry in its working context. >> So, it's not just remembering the conversation where it learned that, it's updating its internal persistent fact sheet. >> Precisely. It's modifying its own state. This is way more robust than just relying on the conversational history because that history might eventually get pushed out of the main context. >> Okay, that makes sense. But wait, if it can edit itself, what stops it from, I don't know, accidentally overwriting its core instructions or forgetting something really important I told it to remember five minutes ago? >> Good question. That's where the architecture comes in. The working context is separate from the readonly system instructions and it's fixed in size, so it can't just grow indefinitely. The LLM is programmed via those system instructions to prioritize certain types of facts and be quite disciplined about when and how it makes edits. It's not random scribbling. It's targeted selfm modification. >> Got it. Discipline selfmodification. Okay. So, system instructions working context. What's the third part of the main context? >> That's the FIFO cue. FIFO stands for first in first out. This is basically the rolling history of the most recent interactions, user messages, the LM's own responses, and importantly, system alerts. >> So, this is the part that's most likely to get full and need trimming. >> Exactly. It's the most volatile section. But they added a really clever trick here. The very first item index zero in this queue isn't a normal message. It's a special system message that contains a recursive summary of all the messages that have already been pushed out of the queue. >> A recursive summary. A summary of summaries >> pretty much. Yeah. As older messages get evicted from the queue to make space, the system automatically summarizes them and updates this summary message at the front. So even when the detailed turnbyturn history is gone from the main context, >> the LLM still has the gist of it, the highlevel narrative thread. >> Exactly. It maintains that long-term narrative context without clogging up the limited main context window with ancient history. It's a smart way to bridge the short term and the potentially long term. Okay, that's the virtual RAM. Now, what about the virtual disc? The external context. You mentioned recall storage and archival storage, >> right? Recall storage is essentially the full unabbridged history. Every single message, every interaction ever, it's stored externally, like in a database. When the LLM needs to find a specific detail from way back, it can query the storage, >> the highfidelity log. >> Yep. And then archival storage is for well archives, huge documents, entire books, databases, website crawls, basically any large text object that wouldn't possibly fit in the main context. Think of that million token annual report example. It lives here. >> So you got the immediate RAM main context with its sections and the massive hard drive external context with recall and archival. >> Who's playing the role of the OS's memory manager coordinating all the swapping? Ah, that's handled by the LLM itself and directed by its internal control flow mechanism, specifically something called the Q manager and the function executive. >> Okay, how does that work? >> The Q manager constantly monitors how full the main context, particularly the FIFO Q, is getting. When the token count hits a certain threshold, say 70% capacity. They call it the warning token count. It triggers an interrupt. It inserts the special memory pressure system alert right into the FIFO Q. >> So, it's like the RAM sending up a flare gun. I'm getting full. do something. >> Exactly. It's an interrupt signal. That alert prompts the LLM, following its system instructions, to take action. It needs to decide, okay, what's critical here? What can I summarize and save to my working context? What less important stuff can I just let go knowing it's in recall storage if needed? Should I push this less critical conversation thread to archival? >> And it uses those MEGPT functions to do this. >> Yes, it generates the necessary function calls. Maybe one to update the working context, maybe another to query archival storage if it needs related info before deciding. Only after the LLM has had a chance to execute these self-directed memory management functions, >> then the system trims the queue. >> Then the Q manager automatically flushes the FIFO queue, typically evicting the oldest half of the messages. The key is that the LLM directs the process before the flush happens. It decides what to retrieve, what to say, what to page out. And crucially, it can even chain multiple function calls together to perform complex tasks. >> That autonomous function chaining, >> that sounds like where the real power comes in beyond just simple memory. Let's look at how this actually performed. They tested it in areas where standard models really struggle, right? Like long conversations. >> Absolutely. Maintaining consistency over days, weeks, or even longer is a huge challenge for standard LLMs. They tested this with a task called deep memory retrieval or DMR. >> Okay. >> The setup was the agent had multiple chat sessions maybe spread over time. Then in a later session it was asked a specific question that required remembering a detail from say session one or two information far outside the current context window. >> And how did a top tier model like GPT4 do on its own without megpt? Maybe just using standard chat history summarization. >> It struggled badly. baseline GBT4, even with some summarization help, only got about 32.1% accuracy recalling those specific long-term facts, it just couldn't reliably access or use the information once it scrolled off the screen, so to speak. >> 32%. >> Okay, now add the MEGPT layer. Same underlying GBT4 model, but with this OSlike memory management, what happened? >> The accuracy skyrocketed. MEM GPT equipped to GPT4 jumped to 92.5% accuracy on the exact same task. >> Wow. 92.5. That's nearly a three-fold increase. >> It's a massive difference. And it proves the point. The underlying model was capable of the reasoning. It just lacked the mechanism to access the necessary information. MEGPT provided that mechanism. >> It's not just about raw recall though, is it? They also looked at how it impacted engagement. >> They did. In a conversation opener task, they had MEGPT use its memory of past interactions to generate personalized opening lines for a new chat session. >> Trying to make it feel less generic, more like resuming a real conversation. >> Exactly. And the results were really positive. Men GPT's generated openers were rated similarly and sometimes even better in quality than openers that were handwritten by humans specifically for the task. It showed it could synthesize that stored information creatively, not just spit back fact. Okay, so it's a huge win for long-term conversations. What about the other big context challenge? Massive documents, >> right? Things like annual reports, technical manuals, legal discovery, stuff that can easily run into millions of tokens, way beyond even the biggest 128k or 200k context windows. >> How did MEGPT tackle that? >> They used a multi-document question answering task. Imagine feeding it a huge database like a dump of Wikipedia articles stored in its archival storage. Memtt could then answer questions that required finding and synthesizing information scattered across potentially many different documents in that external storage. >> So it queries the archive, gets some results back, maybe realizes it needs more queries again iteratively. >> Precisely. It pages the relevant chunks of information from the external disk into its RAM as needed. the effective context size it can handle becomes limited by the external storage and query system, not the LLM's fixed input window. >> That makes sense. But the result that really jumped out at me was the nested key value retrieval task. That sounds complex. >> It is, and it's a fantastic test of that autonomous function chaining we talked about. It simulates multihop reasoning. You can't just ask one question and get the answer. >> How does it work? Imagine a scenario like find the email address of the manager step one for the employee named Bob given okay now using that manager's email address is the key find their department step two now using that department name find the budget code for that department step three each answer is the key for the next query >> okay I see you have to hold the intermediate result formulate the next query execute it get the next result sequentially how did the standard fixed context models handle that >> they completely fell apart the paper shows that bas baseline GPC4 hit 0% accuracy by the time the nesting reached just three levels deep. >> Zero. Why? >> It simply couldn't manage the state. It couldn't reliably hold on to the result from step one while formulating and executing the query for step two within its single fixed prompt structure. It loses the intermediate key or gets confused. It's a fundamental failure point for complex multi-step reasoning in fixed context models. >> Right? and MEGPT same underlying GPT4 model >> completely unaffected me GPT using the exact same GPT4 maintained high accuracy regardless of the number of nesting levels they tested because it can chain those function calls query get result use result as key for next query repeat it handles multihop problems flawlessly >> that's it has incredible it really shows the system turning the LM into something more like a reliable programmable reasoning engine not just a text generator >> exactly it overcomes that sequential reasoning bottleneck So stepping back then, MEGPT feels like this brilliant integration of old school OS concepts, hierarchical memory, interrupts, control flow with modern AI systems. It's tackling the LLM's biggest weakness head-on by abstracting it away. >> It really is. It's providing that crucial layer that allows the LLM's inherent capabilities to shine even when dealing with long time scales or massive amounts of data. This idea of virtual context seems essential for building assistants that actually remember you or for deploying AI in enterprises dealing with huge data volumes. >> And it turns the LLM from a passive recipient into an active system manager. Which brings us to a really fascinating point you mentioned earlier. The control flow isn't just triggered by user messages or memory warnings. >> Right. The MEGBT framework also allows for timed events. events that can trigger the LLM to perform actions on a schedule completely independent of any user interaction at that moment. >> Okay, that opens up a whole different perspective. >> It really does and it leads to maybe the most provocative thought from all this. If you have an LLM that's managing its own memory like an OS, directing its own actions through function calls, handling interrupts, and it can be triggered by internal timers to run tasks unprompted without a human initiating it. >> What does that actually enable? What new class of autonomous agents are we looking at? Agents that aren't just reactively answering our questions, but proactively managing information, performing scheduled tasks, maybe even monitoring things and deciding on their own when to act based on their internal state and stored knowledge. It certainly pushes the boundaries of what we think an AI assistant can be. And that [music] brings us to the end of another deep dive into the world of generative AI. >> Thank you for tuning in to Gen AI LevelUp. If you found this episode valuable, [music] please consider subscribing and leaving a review on your favorite podcast platform. It helps us reach more curious minds like yours. Join us next time as we continue to explore the cutting [music] edge of artificial intelligence. Until then, keep learning, stay curious, and continue to level up.