# Architectural Principles Ledger (V2)## Principle 1: The Four Corners Rule (Absolute Provenance)No data payload is permitted to enter the execution pipeline, be processed by a downstream agent, or be written into a ledger unless it possesses an un-degradable, cryptographic line-of-custody block. Every asset must be tagged with an 8-character SHA256 slice hash linking back to its pristine source index tracking card.## Principle 2: Rigid Behavioral Isolation & Scope QuarantineSpawned agents are fundamentally stateless profiles bound by immutable Crew Manifest Templates. Multi-turn chat memory analysis is authorized for systemic self-correction, but raw file parsing, socket management, and system infrastructure deployments must be locked behind distinct python tool constraints.## Principle 3: The Null-Result ImperativeIf an empirical node, factual assertion, or lineage citation cannot be verified word-for-word against its hash lock wrapper, the system is barred from interpolating fallback filler text. The model must return an unvarnished primitive null state string: "No exact match found".