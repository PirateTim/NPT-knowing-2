# Turn ON immediately:
# gcloud sql instances patch npt-instance-postgressql --activation-policy=ALWAYS

# Turn OFF immediately:
# gcloud sql instances patch npt-instance-postgressql --activation-policy=NEVER